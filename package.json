{
  "name": "map-arduino",
  "version": "1.0.0",
  "description": "Visualizador GPS em tempo real via ESP32 + Leaflet",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "ports": "node -e \"require('serialport').SerialPort.list().then(p => console.log(p.map(x => x.path + ' - ' + x.manufacturer).join('\\n') || 'Nenhuma porta encontrada'))\""
  },
  "dependencies": {
    "@serialport/parser-readline": "^12.0.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "serialport": "^12.0.0"
  }
}
